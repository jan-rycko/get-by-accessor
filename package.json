{
  "name": "get-by-accessor",
  "version": "1.0.0",
  "scripts": {
    "build": "rm -rf lib && tsc -p . && cp src/** lib/",
    "prepublish": "npm run build"
  },
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "license": "MIT",
  "files": [
    "lib/"
  ],
  "devDependencies": {
    "@types/node": "^12.0.8",
    "ts-node": "^8.3.0",
    "typescript": "^3.5.2",
    "typescript-eslint-parser": "^22.0.0"
  }
}
